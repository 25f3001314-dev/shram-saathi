import { useEffect, useMemo, useState } from 'react'
import {
  Activity, ArrowRight, BadgeCheck, BarChart3, Bot, Check, ChevronRight, CircleHelp,
  Clock3, FileCheck2, Headphones, Home, Languages, LayoutDashboard, LockKeyhole,
  Menu, Mic, PhoneCall, Play, Radio, ScanFace, Search, Settings2, ShieldCheck,
  Sparkles, UserRound, UsersRound, Volume2, X, Zap,
} from 'lucide-react'

type Screen = 'home' | 'assistant' | 'services' | 'security' | 'support' | 'dashboard' | 'architecture'
type DemoStep = { label: string; detail: string; icon: typeof Mic }

const demoSteps: DemoStep[] = [
  { label: 'Listening', detail: 'Voice captured in Hindi', icon: Mic },
  { label: 'Understanding', detail: 'Intent: eShram Registration', icon: Sparkles },
  { label: 'Checking documents', detail: 'Government knowledge matched', icon: FileCheck2 },
  { label: 'Verifying identity', detail: 'Voice + camera + OTP simulation', icon: ShieldCheck },
  { label: 'Service completed', detail: 'Confirmation generated', icon: BadgeCheck },
]

const navItems: { id: Screen; label: string; icon: typeof Home }[] = [
  { id: 'home', label: 'Welcome', icon: Home }, { id: 'assistant', label: 'Voice assistant', icon: Mic },
  { id: 'services', label: 'Services', icon: LayoutDashboard }, { id: 'security', label: 'My security', icon: LockKeyhole },
  { id: 'support', label: 'Human support', icon: Headphones }, { id: 'dashboard', label: 'Judge dashboard', icon: BarChart3 },
]

const languages = ['हिन्दी', 'English', 'Bhojpuri', 'Maithili', 'मराठी', 'தமிழ்', 'తెలుగు', 'বাংলা', 'ગુજરાતી', 'ಕನ್ನಡ', 'ਪੰਜਾਬੀ', 'ଓଡ଼ିଆ']

function App() {
  const [screen, setScreen] = useState<Screen>('home')
  const [language, setLanguage] = useState('हिन्दी')
  const [isNavOpen, setIsNavOpen] = useState(false)
  const [isListening, setIsListening] = useState(false)
  const [demoActive, setDemoActive] = useState(false)
  const [demoStep, setDemoStep] = useState(-1)
  const [showLanguages, setShowLanguages] = useState(false)
  const [showToast, setShowToast] = useState('')

  const startDemo = () => {
    setScreen('assistant'); setDemoActive(true); setDemoStep(0)
  }

  useEffect(() => {
    if (!demoActive || demoStep < 0) return
    if (demoStep >= demoSteps.length) { setDemoActive(false); return }
    const timer = window.setTimeout(() => setDemoStep((step) => step + 1), 1150)
    return () => window.clearTimeout(timer)
  }, [demoActive, demoStep])

  const navigate = (next: Screen) => { setScreen(next); setIsNavOpen(false) }
  const toast = (message: string) => { setShowToast(message); window.setTimeout(() => setShowToast(''), 3000) }
  const screenTitle = navItems.find((item) => item.id === screen)?.label || 'Welcome'

  return <div className="app-shell">
    <aside className={`sidebar ${isNavOpen ? 'open' : ''}`}>
      <div className="brand"><div className="brand-mark"><Volume2 size={18} /></div><div><strong>Shram Saathi</strong><span>Voice access for every worker</span></div></div>
      <div className="demo-badge"><span className="pulse-dot" /> DEMO / SIMULATION</div>
      <nav aria-label="Main navigation">
        <p className="nav-label">Worker experience</p>
        {navItems.slice(0, 5).map(({ id, label, icon: Icon }) => <button key={id} className={`nav-item ${screen === id ? 'active' : ''}`} onClick={() => navigate(id)}><Icon size={18} /><span>{label}</span>{screen === id && <ChevronRight className="nav-chevron" size={15} />}</button>)}
        <p className="nav-label admin-label">For judges & partners</p>
        <button className={`nav-item ${screen === 'dashboard' ? 'active' : ''}`} onClick={() => navigate('dashboard')}><BarChart3 size={18} /><span>Judge dashboard</span></button>
        <button className={`nav-item ${screen === 'architecture' ? 'active' : ''}`} onClick={() => navigate('architecture')}><Activity size={18} /><span>How it works</span></button>
      </nav>
      <div className="sidebar-bottom"><div className="help-mini"><div className="help-icon"><PhoneCall size={17} /></div><div><b>Need a human?</b><span>Someone can help you</span></div></div><button className="sidebar-help" onClick={() => navigate('support')}>Talk to a human <ArrowRight size={15} /></button></div>
    </aside>
    <main className="main-content">
      <header className="topbar"><button className="mobile-menu" aria-label="Open menu" onClick={() => setIsNavOpen(!isNavOpen)}><Menu size={22} /></button><div className="breadcrumb"><span>Shram Saathi</span><ChevronRight size={14} /><b>{screenTitle}</b></div><div className="top-actions"><div className="network"><span className="network-dot" /> Demo network online</div><button className="language-button" onClick={() => setShowLanguages(!showLanguages)}><Languages size={17} /> {language}<ChevronRight size={14} className={showLanguages ? 'rotate-90' : ''} /></button>{showLanguages && <LanguagePopover language={language} setLanguage={setLanguage} close={() => setShowLanguages(false)} />}</div></header>
      <div className="page-wrap">
        {screen === 'home' && <HomeScreen startDemo={startDemo} navigate={navigate} />}
        {screen === 'assistant' && <AssistantScreen isListening={isListening} setIsListening={setIsListening} demoActive={demoActive} demoStep={demoStep} startDemo={startDemo} toast={toast} />}
        {screen === 'services' && <ServicesScreen navigate={navigate} />}
        {screen === 'security' && <SecurityScreen toast={toast} />}
        {screen === 'support' && <SupportScreen toast={toast} />}
        {screen === 'dashboard' && <DashboardScreen />}
        {screen === 'architecture' && <ArchitectureScreen />}
      </div>
    </main>
    {showToast && <div className="toast"><Check size={17} /> {showToast}</div>}
  </div>
}

function LanguagePopover({ language, setLanguage, close }: { language: string; setLanguage: (value: string) => void; close: () => void }) {
  return <div className="language-popover"><div className="popover-head"><b>Choose your language</b><button onClick={close} aria-label="Close"><X size={15} /></button></div><div className="language-grid">{languages.map((item) => <button className={language === item ? 'selected' : ''} key={item} onClick={() => { setLanguage(item); close() }}>{language === item && <Check size={13} />}{item}</button>)}</div></div>
}

function HomeScreen({ startDemo, navigate }: { startDemo: () => void; navigate: (screen: Screen) => void }) {
  return <div className="home-screen"><section className="hero-section"><div className="hero-copy"><div className="eyebrow"><span className="eyebrow-line" /> BUILT FOR EVERY WORKER</div><h1>Har Mazdoor Ka<br /><em>Adhikar,</em> Ab Awaaz Mein.</h1><p className="hero-lede">Access labour services, understand your rights, and get support simply by speaking in your language.</p><div className="hero-actions"><button className="primary-btn hero-btn" onClick={() => navigate('assistant')}><Mic size={20} /> बोलकर शुरू करें <ArrowRight size={17} /></button><button className="secondary-btn" onClick={startDemo}><Play size={16} fill="currentColor" /> Start 60-second demo</button></div><div className="hero-trust"><span><Check size={14} /> No app required</span><span><Check size={14} /> Demo data only</span><span><Check size={14} /> Human support</span></div></div><WorkerVisual /></section><section className="channel-strip"><div className="strip-intro"><span>ONE ACCESS LAYER</span><b>Meet workers where they are</b></div><div className="channels"><Channel icon={<PhoneCall size={19} />} label="WhatsApp" /><Channel icon={<Radio size={19} />} label="Toll-free IVR" /><Channel icon={<UsersRound size={19} />} label="CSC kiosk" /><Channel icon={<Mic size={19} />} label="Voice-first" /></div></section><section className="home-lower"><div><p className="section-kicker">A simpler way in</p><h2>Less typing.<br />More <span>doing.</span></h2></div><div className="benefit-grid"><Benefit icon={<Languages />} title="Regional languages" text="Speak in the language that feels like home." /><Benefit icon={<Zap />} title="Fast, even offline" text="Designed for low-connectivity environments." /><Benefit icon={<ShieldCheck />} title="Private by design" text="Masked demo data and layered verification." /><Benefit icon={<Headphones />} title="Never stuck alone" text="A human can join whenever you need one." /></div></section></div>
}

function WorkerVisual() { return <div className="worker-visual"><div className="visual-glow" /><div className="visual-card top-card"><div className="mini-wave"><span /><span /><span /><span /><span /><span /><span /></div><div><b>Listening...</b><small>Hindi detected</small></div><span className="green-live" /></div><div className="worker-portrait"><div className="sun-disc" /><div className="worker-head" /><div className="worker-body" /><div className="worker-scarf" /><div className="worker-arm left" /><div className="worker-arm right" /><div className="worker-tool" /></div><div className="visual-card bottom-card"><div className="success-check"><Check size={15} /></div><div><b>Service complete</b><small>DEMO-ESHRAM-2026-001</small></div></div><div className="visual-caption"><span>01</span><span className="caption-line" /><b>One voice. Many doors.</b></div></div> }
function Channel({ icon, label }: { icon: React.ReactNode; label: string }) { return <div className="channel"><span>{icon}</span><b>{label}</b></div> }
function Benefit({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <div className="benefit"><div className="benefit-icon">{icon}</div><div><b>{title}</b><p>{text}</p></div></div> }

function AssistantScreen({ isListening, setIsListening, demoActive, demoStep, startDemo, toast }: { isListening: boolean; setIsListening: (value: boolean) => void; demoActive: boolean; demoStep: number; startDemo: () => void; toast: (message: string) => void }) {
  const currentStep = demoSteps[Math.min(demoStep, demoSteps.length - 1)]
  return <div className="assistant-screen"><div className="screen-heading"><div><p className="section-kicker">Your voice is the interface</p><h1>What can we help<br />you <span>with today?</span></h1></div><div className="privacy-note"><LockKeyhole size={16} /><span>Private demo session<br /><b>Nothing is stored</b></span></div></div><div className="assistant-grid"><section className="voice-panel"><div className="panel-top"><span className="status-label"><span className="status-dot" /> {isListening ? 'Listening now' : 'Ready when you are'}</span><span className="session-label">SESSION #SS-2401</span></div><div className={`voice-stage ${isListening ? 'recording' : ''}`}><div className="voice-rings"><div /><div /><div /></div><button className="mic-button" aria-label={isListening ? 'Stop listening' : 'Start listening'} onClick={() => { setIsListening(!isListening); toast(isListening ? 'Voice paused' : 'Demo microphone ready') }}><Mic size={42} /></button><div className="stage-label">{isListening ? 'Listening...' : 'Tap to speak'}</div><div className="waveform">{Array.from({ length: 25 }, (_, i) => <i key={i} style={{ height: `${isListening ? 12 + ((i * 17) % 30) : 5 + ((i * 7) % 11)}px`, animationDelay: `${i * 0.04}s` }} />)}</div></div><div className="sample-utterance"><div className="quote-mark">“</div><div><span>Try saying</span><p>Mujhe eShram mein registration karna hai.</p></div><Volume2 size={19} /></div><div className="assistant-actions"><button className="secondary-btn" onClick={() => toast('Response repeated in Hindi')}><Volume2 size={16} /> Repeat</button><button className="text-btn" onClick={() => toast('Human assistance request started')}><Headphones size={16} /> Talk to human</button></div></section><section className="conversation-panel"><div className="conversation-head"><div><p className="section-kicker">Live conversation</p><h3>Your assistant</h3></div><span className="language-chip"><Languages size={14} /> Hindi</span></div><div className="conversation-body"><div className="message worker"><div className="message-avatar worker-avatar"><UserRound size={15} /></div><div><small>You said</small><p>Mujhe eShram mein registration karna hai.</p></div></div><div className="message assistant"><div className="message-avatar assistant-avatar"><Bot size={15} /></div><div><small>Shram Saathi <span>· just now</span></small><p>Bilkul. Main aapko eShram registration mein madad karta hoon.</p><button className="listen-response"><Volume2 size={14} /> Listen to response</button></div></div>{demoActive && demoStep < demoSteps.length && <div className="processing-message"><div className="processing-spinner" /><div><b>{currentStep?.label}</b><span>{currentStep?.detail}</span></div></div>}</div><button className="primary-btn full-btn" onClick={demoActive ? undefined : startDemo}>{demoActive ? <><Activity size={17} /> Processing demo...</> : <><Play size={17} /> Start guided demo</>}</button></section></div>{demoActive && <DemoTimeline step={demoStep} />}</div>
}

function DemoTimeline({ step }: { step: number }) { return <section className="demo-timeline"><div className="timeline-heading"><div><span className="live-tag"><span className="status-dot" /> LIVE DEMO</span><h3>From voice to service</h3></div><span className="timeline-time">{Math.min(60, Math.max(0, step * 12))} / 60 sec</span></div><div className="timeline-track">{demoSteps.map((item, index) => { const Icon = item.icon; const done = index < step; const current = index === step; return <div className={`timeline-step ${done ? 'done' : ''} ${current ? 'current' : ''}`} key={item.label}><div className="timeline-icon">{done ? <Check size={15} /> : <Icon size={15} />}</div><b>{item.label}</b><span>{item.detail}</span>{index < demoSteps.length - 1 && <div className="track-line" />}</div> })}</div>{step >= demoSteps.length && <div className="completion-banner"><BadgeCheck size={21} /><div><b>eShram Registration Successful</b><span>Registration ID: DEMO-ESHRAM-2026-001 · Demo data</span></div><Check size={20} /></div>}</section> }

function ServicesScreen({ navigate }: { navigate: (screen: Screen) => void }) { const services = [{ icon: <FileCheck2 />, title: 'eShram registration', text: 'Get your worker registration started', tone: 'teal' }, { icon: <Activity />, title: 'PF / EPFO help', text: 'Balance, claims and UAN guidance', tone: 'blue' }, { icon: <ShieldCheck />, title: 'ESI services', text: 'Healthcare and insurance support', tone: 'orange' }, { icon: <Search />, title: 'Find a job', text: 'Browse NCS opportunities near you', tone: 'green' }, { icon: <Settings2 />, title: 'Shram Suvidha', text: 'Compliance and application support', tone: 'purple' }, { icon: <UserRound />, title: 'My profile', text: 'Your services and requests', tone: 'rose' }, { icon: <Clock3 />, title: 'Application status', text: 'Track something you applied for', tone: 'sky' }, { icon: <CircleHelp />, title: 'Help & support', text: 'Get answers or talk to a human', tone: 'gold' }]; return <div className="services-screen"><div className="screen-heading compact"><div><p className="section-kicker">Choose a service</p><h1>What do you need<br />help <span>with?</span></h1></div><button className="primary-btn" onClick={() => navigate('assistant')}><Mic size={17} /> Ask by voice</button></div><div className="service-grid">{services.map((service) => <button className="service-card" key={service.title} onClick={() => navigate(service.title === 'Help & support' ? 'support' : 'assistant')}><div className={`service-icon ${service.tone}`}>{service.icon}</div><div><h3>{service.title}</h3><p>{service.text}</p></div><ArrowRight className="card-arrow" size={18} /></button>)}</div><div className="services-foot"><div className="source-badge"><FileCheck2 size={17} /><span><b>Government-document grounded</b><small>Answers are based on the local demo knowledge base</small></span></div><div className="service-voice-hint"><Mic size={17} /> You can say: “Mujhe PF status batayein”</div></div></div> }

function SecurityScreen({ toast }: { toast: (message: string) => void }) { const layers = [{ icon: <Volume2 />, title: 'Voice liveness', text: 'Say a short phrase so we know a real person is speaking.', state: 'Verified' }, { icon: <ScanFace />, title: 'Camera liveness', text: 'A quick camera check confirms you are present.', state: 'Verified' }, { icon: <LockKeyhole />, title: 'One-time password', text: 'A code adds one more layer of protection.', state: 'Verified' }]; return <div className="security-screen"><div className="screen-heading compact"><div><p className="section-kicker">Your safety comes first</p><h1>Three simple checks.<br /><span>One safer service.</span></h1></div><div className="simulation-tag"><ShieldCheck size={17} /> Simulation / Demo</div></div><div className="security-layout"><section className="auth-card"><div className="auth-card-head"><div><span className="eyebrow tiny">AUTHENTICATION SESSION</span><h3>Identity verification</h3></div><span className="verified-pill"><Check size={14} /> 3 of 3 complete</span></div>{layers.map((layer, index) => <div className="auth-layer" key={layer.title}><div className="layer-number">0{index + 1}</div><div className="layer-icon">{layer.icon}</div><div className="layer-copy"><b>{layer.title}</b><p>{layer.text}</p></div><div className="layer-state"><Check size={14} /> {layer.state}</div></div>)}<button className="primary-btn full-btn" onClick={() => toast('Demo authentication already verified')}><BadgeCheck size={17} /> Continue to service</button></section><aside className="privacy-card"><div className="privacy-seal"><ShieldCheck size={27} /></div><p className="section-kicker">Privacy by design</p><h3>Your information stays yours.</h3><p>This prototype never uses real Aadhaar or personal worker data. Sensitive numbers are masked before they appear in the interface.</p><div className="masked-id"><small>DEMO AADHAAR</small><b>XXXX-XXXX-1234</b></div><span className="privacy-foot"><LockKeyhole size={14} /> AES-256-GCM architecture ready</span></aside></div></div> }

function SupportScreen({ toast }: { toast: (message: string) => void }) { return <div className="support-screen"><div className="support-hero"><div className="support-orb"><Headphones size={35} /></div><div><p className="section-kicker">You do not have to figure it out alone</p><h1>Talk to a human<br /><span>when you need one.</span></h1><p className="support-lede">A trained CSC VLE can join your conversation, explain the next step, and help you complete the service.</p></div></div><div className="support-grid"><section className="handoff-card"><div className="handoff-head"><span className="live-tag"><span className="status-dot" /> AGENT NETWORK</span><span className="available"><span /> 24/7 designed availability</span></div><div className="handoff-flow"><FlowNode label="You" icon={<UserRound />} /><div className="flow-arrow"><ArrowRight /></div><FlowNode label="AI assistant" icon={<Bot />} /><div className="flow-arrow"><ArrowRight /></div><FlowNode label="Human / VLE" icon={<Headphones />} active /></div><div className="agent-ready"><div className="agent-avatars"><span>AK</span><span>RP</span><span>+</span></div><div><b>Human support is available</b><p>Average wait time: <strong>02 minutes</strong></p></div><button className="primary-btn" onClick={() => toast('Your human support request is in the demo queue')}>Connect me <PhoneCall size={16} /></button></div></section><section className="support-options"><Option icon={<PhoneCall />} title="Request a callback" text="A VLE calls you on your phone." onClick={() => toast('Callback requested for demo')} /><Option icon={<UsersRound />} title="Find a CSC centre" text="Get help in person, near you." onClick={() => toast('Showing demo CSC locations')} /><Option icon={<Bot />} title="Continue with AI" text="Keep going at your own pace." onClick={() => toast('Returning to AI assistant')} /></section></div></div> }
function FlowNode({ label, icon, active }: { label: string; icon: React.ReactNode; active?: boolean }) { return <div className={`flow-node ${active ? 'active' : ''}`}><div>{icon}</div><b>{label}</b></div> }
function Option({ icon, title, text, onClick }: { icon: React.ReactNode; title: string; text: string; onClick: () => void }) { return <button className="option-row" onClick={onClick}><div className="option-icon">{icon}</div><div><b>{title}</b><p>{text}</p></div><ChevronRight size={18} /></button> }

function DashboardScreen() { const metrics = [{ label: 'Total requests', value: '12,480', note: '+18% this demo', icon: <Activity />, tone: 'blue' }, { label: 'Voice sessions', value: '9,870', note: '79% of requests', icon: <Mic />, tone: 'teal' }, { label: 'Languages used', value: '14', note: 'Expandable by design', icon: <Languages />, tone: 'orange' }, { label: 'Successful services', value: '96.8%', note: 'Simulated metric', icon: <BadgeCheck />, tone: 'green' }, { label: 'Human escalations', value: '412', note: '3.3% of requests', icon: <Headphones />, tone: 'rose' }, { label: 'Auth success', value: '98.7%', note: 'Demo flow only', icon: <ShieldCheck />, tone: 'purple' }]; return <div className="dashboard-screen"><div className="dashboard-head"><div><div className="eyebrow"><span className="eyebrow-line" /> OPERATIONS VIEW</div><h1>The access layer,<br /><span>at a glance.</span></h1><p>Signals from the Shram Saathi demo gateway.</p></div><div className="dashboard-tag"><Activity size={16} /> Demo / Simulated Data</div></div><div className="metric-grid">{metrics.map((metric) => <div className="metric-card" key={metric.label}><div className={`metric-icon ${metric.tone}`}>{metric.icon}</div><span>{metric.label}</span><strong>{metric.value}</strong><small>{metric.note}</small></div>)}</div><div className="dashboard-lower"><section className="chart-card"><div className="card-heading"><div><p className="section-kicker">Gateway activity</p><h3>Requests by channel</h3></div><span className="period-chip">Today <ChevronRight size={13} /></span></div><div className="bar-chart">{[['WhatsApp', 82, '9,870'], ['Voice web', 63, '7,240'], ['CSC kiosk', 44, '5,030'], ['Toll-free IVR', 31, '3,520']].map(([label, percent, value]) => <div className="bar-row" key={label as string}><div><span>{label}</span><b>{value}</b></div><div className="bar-track"><i style={{ width: `${percent}%` }} /></div></div>)}</div></section><section className="activity-card"><div className="card-heading"><div><p className="section-kicker">System pulse</p><h3>Live activity</h3></div><span className="live-tag"><span className="status-dot" /> LIVE</span></div><div className="activity-list"><ActivityRow color="teal" title="eShram registration completed" time="just now" /><ActivityRow color="orange" title="Hindi language session started" time="1 min ago" /><ActivityRow color="blue" title="Human handoff queued" time="2 min ago" /><ActivityRow color="green" title="Semantic cache hit" time="3 min ago" /></div><div className="cache-note"><Zap size={15} /><span><b>Cache is helping</b> · repeated questions can be answered faster</span></div></section></div><div className="dashboard-footer"><span><LockKeyhole size={14} /> No production data connected</span><span><Check size={14} /> Mock adapters are replaceable</span><span><Radio size={14} /> API gateway healthy</span></div></div> }
function ActivityRow({ color, title, time }: { color: string; title: string; time: string }) { return <div className="activity-row"><span className={`activity-dot ${color}`} /><div><b>{title}</b><small>{time}</small></div><ChevronRight size={15} /></div> }

function ArchitectureScreen() { const nodes = ['Worker', 'WhatsApp / IVR / CSC', 'API Gateway', 'Voice + noise layer', 'Language + translation', 'AI / RAG', 'Auth + action', 'Mock government APIs']; return <div className="architecture-screen"><div className="screen-heading compact"><div><p className="section-kicker">Built to connect, not replace</p><h1>One friendly layer<br /><span>over many systems.</span></h1></div><div className="architecture-note"><Activity size={17} /><span>Middleware architecture<br /><b>Ready for real providers</b></span></div></div><div className="architecture-board"><div className="flow-column">{nodes.map((node, index) => <div className="arch-node-wrap" key={node}><div className={`arch-node ${index === 0 ? 'worker-node' : index === nodes.length - 1 ? 'gov-node' : ''}`}><span className="arch-index">0{index + 1}</span><b>{node}</b>{index === 3 && <small>BHASHINI / Sarvam adapters</small>}{index === 5 && <small>Government documents only</small>}{index === 7 && <small>eShram · NCS · Shram Suvidha</small>}</div>{index < nodes.length - 1 && <div className="arch-connector"><span /></div>}</div>)}</div><div className="side-architecture"><div className="side-module"><div className="module-icon cache"><Zap size={18} /></div><div><b>Semantic cache</b><p>Qdrant-ready. Mock cache for this demo.</p></div></div><div className="side-module"><div className="module-icon ocr"><ScanFace size={18} /></div><div><b>OCR service</b><p>OpenCV → CRAFT → Tesseract → structured data.</p></div></div><div className="side-module"><div className="module-icon human"><Headphones size={18} /></div><div><b>Human co-pilot</b><p>Confused or distressed? A VLE can join.</p></div></div><div className="side-module"><div className="module-icon audit"><FileCheck2 size={18} /></div><div><b>Audit logger</b><p>Every demo action leaves a trace.</p></div></div></div></div><div className="architecture-disclaimer"><ShieldCheck size={16} /><span>Prototype boundary: government services are simulated. The gateway is designed to make future integrations replaceable and auditable.</span></div></div> }

export default App